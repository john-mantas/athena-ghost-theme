{{!--

Properties:
- show-accent-color="true": Enables the background color of the card.
- fallback-icon: The icon name to be used when the tag has no featured image,
	valid values are only the filenames of the icons in partials/icons folder.

--}}

<a
	href="{{url}}"
	class="card-tag card-tag--{{slug}}"
	data-image-size="{{@custom.tags_card_image_style}}"
	{{#if show-accent-color}}{{#if accent_color}} style="--accent-color: {{accent_color}}"{{/if}}{{/if}}
>
	{{#match @custom.tags_card_image_style "!=" "Hidden"}}
		<figure class="card-tag__image">
			{{#if feature_image}}
				<img
					srcset="
					{{img_url feature_image size="xxs"}} 30w,
					{{img_url feature_image size="xs"}} 100w,
					{{img_url feature_image size="s"}} 300w,
					{{img_url feature_image size="m"}} 600w,
					{{img_url feature_image size="l"}} 1200w"
					sizes="(min-width: 1200px) 1200px, 90vw"
					src="{{img_url feature_image size="l"}}"
					alt="{{#if feature_image_alt}}{{feature_image_alt}}{{/if}}"
				>
			{{else}}
				{{#match @custom.tags_card_image_style "!=" "Full width"}}
					{{#if fallback-icon}}
						{{> (concat "icons/" fallback-icon)}}
					{{/if}}
				{{/match}}
			{{/if}}
		</figure>
	{{/match}}
	<div class="card-tag__content">
		<h2 class="card-tag__name">{{name}}</h2>
		{{#if description}}
			<div class="card-tag__description">{{description}}</div>
		{{/if}}
	</div>
</a>
